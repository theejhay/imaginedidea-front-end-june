function add (a, b) {
    return a + b;
}

function multiply (a, b) {
    return a * b;
}

const pi = 3.142;

const square = (x) => x * x;

const cube = (x) => x * x * x;

export { add, multiply, pi, square, cube };

export default { add };
